{
  "name": "traffic-node-proxy",
  "version": "1.0.0",
  "description": "Edge device upload proxy — receives 26-dim attribute JSON, queues via Redis, writes to MySQL",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "mysql2": "^3.9.4",
    "dotenv": "^16.4.5"
  }
}
